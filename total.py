#total = sum(float(input(f"Enter {vars} ")) for car in "abcd")
#print(total)

total = sum(float(input(f"enter {var}: ")) for var in "abcd")
print (total)


total = sum(float(input(f"Enter {var}: ")) for var in "ab")
print(total)